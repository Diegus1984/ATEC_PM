<Window
    x:Class="ATEC.PM.Client.Views.EmployeeDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Utente"
    Width="560"
    Height="720"
    Background="{DynamicResource ShadcnBackgroundBrush}"
    FontFamily="{DynamicResource ShadcnFontFamily}"
    ResizeMode="CanResize"
    WindowStartupLocation="CenterOwner">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
            </ResourceDictionary.MergedDictionaries>
            
            <Style x:Key="FieldLabel" TargetType="TextBlock">
                <Setter Property="FontSize" Value="11.5" />
                <Setter Property="FontWeight" Value="SemiBold" />
                <Setter Property="Foreground" Value="{DynamicResource ShadcnMutedForegroundBrush}" />
                <Setter Property="Margin" Value="0,10,0,4" />
            </Style>
            
            <Style x:Key="FieldInput" TargetType="TextBox" BasedOn="{StaticResource ShadcnTextBox}">
                <Setter Property="Height" Value="32" />
            </Style>

            <Style x:Key="FieldPassword" TargetType="PasswordBox" BasedOn="{StaticResource ShadcnPasswordBox}">
                <Setter Property="Height" Value="32" />
            </Style>
            
            <Style x:Key="ExpHeader" TargetType="TextBlock">
                <Setter Property="FontSize" Value="12.5" />
                <Setter Property="FontWeight" Value="Bold" />
                <Setter Property="Foreground" Value="{DynamicResource ShadcnForegroundBrush}" />
            </Style>
            
            <Style x:Key="ExpBorder" TargetType="Border" BasedOn="{StaticResource ShadcnCard}">
                <Setter Property="Padding" Value="16,12" />
                <Setter Property="Margin" Value="0,0,0,8" />
            </Style>
        </ResourceDictionary>
    </Window.Resources>

    <DockPanel>
        <!--  BOTTOM BUTTONS  -->
        <Border
            Padding="16,12"
            Background="White"
            BorderBrush="{DynamicResource ShadcnBorderBrush}"
            BorderThickness="0,1,0,0"
            DockPanel.Dock="Bottom">
            <DockPanel>
                <TextBlock
                    x:Name="txtError"
                    MaxWidth="300"
                    VerticalAlignment="Center"
                    DockPanel.Dock="Left"
                    FontSize="12"
                    Foreground="{DynamicResource ShadcnDestructiveBrush}"
                    TextWrapping="Wrap" />
                <StackPanel
                    HorizontalAlignment="Right"
                    DockPanel.Dock="Right"
                    Orientation="Horizontal">
                    <Button
                        Width="90"
                        Height="34"
                        Click="BtnCancel_Click"
                        Content="Annulla"
                        Style="{StaticResource ShadcnButtonOutline}"
                        IsCancel="True" />
                    <Button
                        x:Name="btnSave"
                        Width="110"
                        Height="34"
                        Margin="8,0,0,0"
                        Click="BtnSave_Click"
                        Content="Salva"
                        Style="{StaticResource ShadcnButtonDefault}"
                        IsDefault="True" />
                </StackPanel>
            </DockPanel>
        </Border>

        <!--  FORM  -->
        <ScrollViewer Padding="16,12,16,8" VerticalScrollBarVisibility="Auto">
            <StackPanel>

                <!--  ═══ ANAGRAFICA ═══  -->
                <Expander
                    x:Name="expAnagrafica"
                    Margin="0,0,0,8"
                    IsExpanded="True">
                    <Expander.Header>
                        <Border Padding="0,6" Background="Transparent">
                            <TextBlock Style="{StaticResource ExpHeader}" Text="👤  ANAGRAFICA" />
                        </Border>
                    </Expander.Header>
                    <Border Style="{StaticResource ExpBorder}">
                        <StackPanel>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="12" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0">
                                    <TextBlock Style="{StaticResource FieldLabel}" Text="NOME *" />
                                    <TextBox x:Name="txtFirstName" Style="{StaticResource FieldInput}" />
                                </StackPanel>
                                <StackPanel Grid.Column="2">
                                    <TextBlock Style="{StaticResource FieldLabel}" Text="COGNOME *" />
                                    <TextBox x:Name="txtLastName" Style="{StaticResource FieldInput}" />
                                </StackPanel>
                            </Grid>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="12" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0">
                                    <TextBlock Style="{StaticResource FieldLabel}" Text="EMAIL" />
                                    <TextBox x:Name="txtEmail" Style="{StaticResource FieldInput}" />
                                </StackPanel>
                                <StackPanel Grid.Column="2">
                                    <TextBlock Style="{StaticResource FieldLabel}" Text="TIPO" />
                                    <ComboBox
                                        x:Name="cmbType"
                                        Style="{StaticResource ShadcnComboBox}"
                                        Height="32">
                                        <ComboBoxItem Content="INTERNAL" IsSelected="True" />
                                        <ComboBoxItem Content="EXTERNAL" />
                                    </ComboBox>
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                    </Border>
                </Expander>

                <!--  ═══ ACCESSO E RUOLO ═══  -->
                <Expander
                    x:Name="expAccesso"
                    Margin="0,0,0,8"
                    IsExpanded="True">
                    <Expander.Header>
                        <Border Padding="0,6" Background="Transparent">
                            <TextBlock Style="{StaticResource ExpHeader}" Text="🔐  ACCESSO E RUOLO" />
                        </Border>
                    </Expander.Header>
                    <Border Style="{StaticResource ExpBorder}">
                        <StackPanel>
                            <!--  Ruolo  -->
                            <TextBlock Style="{StaticResource FieldLabel}" Text="RUOLO SISTEMA" />
                            <Border
                                Padding="12,8"
                                Background="{DynamicResource ShadcnSecondaryBrush}"
                                BorderBrush="{DynamicResource ShadcnBorderBrush}"
                                BorderThickness="1"
                                CornerRadius="6"
                                Margin="0,0,0,8">
                                <UniformGrid Columns="2" Rows="2">
                                    <RadioButton
                                        x:Name="rbTech"
                                        Margin="0,4"
                                        Content="TECH — Tecnico operativo"
                                        FontSize="12"
                                        GroupName="Role"
                                        IsChecked="True" />
                                    <RadioButton
                                        x:Name="rbResp"
                                        Margin="0,4"
                                        Content="RESP — Responsabile reparto"
                                        FontSize="12"
                                        GroupName="Role" />
                                    <RadioButton
                                        x:Name="rbPm"
                                        Margin="0,4"
                                        Content="PM — Project Manager"
                                        FontSize="12"
                                        GroupName="Role" />
                                    <RadioButton
                                        x:Name="rbAdmin"
                                        Margin="0,4"
                                        Content="ADMIN — Accesso totale"
                                        FontSize="12"
                                        GroupName="Role" />
                                </UniformGrid>
                            </Border>
                            <!--  Credenziali  -->
                            <TextBlock Style="{StaticResource FieldLabel}" Text="USERNAME" />
                            <TextBox x:Name="txtUsername" Style="{StaticResource FieldInput}" />
                            <Grid Margin="0,0,0,4">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="12" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0">
                                    <TextBlock Style="{StaticResource FieldLabel}" Text="PASSWORD" />
                                    <PasswordBox
                                        x:Name="txtPassword"
                                        Style="{StaticResource FieldPassword}" />
                                </StackPanel>
                                <StackPanel Grid.Column="2">
                                    <TextBlock Style="{StaticResource FieldLabel}" Text="CONFERMA PASSWORD" />
                                    <PasswordBox
                                        x:Name="txtPasswordConfirm"
                                        Style="{StaticResource FieldPassword}" />
                                </StackPanel>
                            </Grid>
                            <TextBlock
                                x:Name="txtPasswordHint"
                                Margin="0,4,0,10"
                                FontSize="11"
                                Foreground="{DynamicResource ShadcnMutedForegroundBrush}"
                                Text="Lascia vuoto per non modificare la password" />
                            <!--  Stato  -->
                            <TextBlock Style="{StaticResource FieldLabel}" Text="STATO" />
                            <ComboBox
                                x:Name="cmbStatus"
                                Style="{StaticResource ShadcnComboBox}"
                                Height="32">
                                <ComboBoxItem Content="ACTIVE" IsSelected="True" />
                                <ComboBoxItem Content="ON_LEAVE" />
                                <ComboBoxItem Content="SICK" />
                                <ComboBoxItem Content="TERMINATED" />
                            </ComboBox>
                        </StackPanel>
                    </Border>
                </Expander>

                <!--  ═══ REPARTI ═══  -->
                <Expander
                    x:Name="expReparti"
                    Margin="0,0,0,8"
                    IsExpanded="False">
                    <Expander.Header>
                        <Border Padding="0,6" Background="Transparent">
                            <TextBlock Style="{StaticResource ExpHeader}" Text="🏭  REPARTI DI APPARTENENZA" />
                        </Border>
                    </Expander.Header>
                    <Border Style="{StaticResource ExpBorder}">
                        <StackPanel>
                            <Grid Margin="0,0,0,8">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="24" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="80" />
                                    <ColumnDefinition Width="80" />
                                </Grid.ColumnDefinitions>
                                <TextBlock
                                    Grid.Column="1"
                                    FontSize="11.5"
                                    FontWeight="SemiBold"
                                    Foreground="{DynamicResource ShadcnMutedForegroundBrush}"
                                    Text="REPARTO" />
                                <TextBlock
                                    Grid.Column="2"
                                    HorizontalAlignment="Center"
                                    FontSize="11.5"
                                    FontWeight="SemiBold"
                                    Foreground="{DynamicResource ShadcnMutedForegroundBrush}"
                                    Text="★ RESP." />
                                <TextBlock
                                    Grid.Column="3"
                                    HorizontalAlignment="Center"
                                    FontSize="11.5"
                                    FontWeight="SemiBold"
                                    Foreground="{DynamicResource ShadcnMutedForegroundBrush}"
                                    Text="◆ PRIM." />
                            </Grid>
                            <ItemsControl x:Name="lstDeptMembership">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Height="28" Margin="0,3">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="24" />
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="80" />
                                                <ColumnDefinition Width="80" />
                                            </Grid.ColumnDefinitions>
                                            <CheckBox
                                                Grid.Column="0"
                                                VerticalAlignment="Center"
                                                IsChecked="{Binding IsMember}" />
                                            <TextBlock
                                                Grid.Column="1"
                                                VerticalAlignment="Center"
                                                FontSize="13"
                                                Text="{Binding DepartmentName}" />
                                            <CheckBox
                                                Grid.Column="2"
                                                HorizontalAlignment="Center"
                                                VerticalAlignment="Center"
                                                IsChecked="{Binding IsResponsible}"
                                                IsEnabled="{Binding IsMember}" />
                                            <CheckBox
                                                Grid.Column="3"
                                                HorizontalAlignment="Center"
                                                VerticalAlignment="Center"
                                                IsChecked="{Binding IsPrimary}"
                                                IsEnabled="{Binding IsMember}" />
                                        </Grid>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </Border>
                </Expander>

                <!--  ═══ COMPETENZE ═══  -->
                <Expander
                    x:Name="expCompetenze"
                    Margin="0,0,0,8"
                    IsExpanded="False">
                    <Expander.Header>
                        <Border Padding="0,6" Background="Transparent">
                            <TextBlock Style="{StaticResource ExpHeader}" Text="⚙️  COMPETENZE TECNICHE" />
                        </Border>
                    </Expander.Header>
                    <Border Style="{StaticResource ExpBorder}">
                        <StackPanel>
                            <TextBlock
                                Margin="0,0,0,10"
                                FontSize="11.5"
                                Foreground="{DynamicResource ShadcnMutedForegroundBrush}"
                                Text="Reparti su cui è tecnicamente abilitato (anche senza appartenenza formale)" />
                            <ItemsControl x:Name="lstCompetences">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <StackPanel Margin="0,4">
                                            <CheckBox
                                                Content="{Binding DepartmentName}"
                                                FontSize="13"
                                                IsChecked="{Binding IsEnabled}" />
                                            <TextBox
                                                Height="28"
                                                Margin="22,4,0,0"
                                                Style="{StaticResource ShadcnTextBox}"
                                                IsEnabled="{Binding IsEnabled}"
                                                Opacity="{Binding IsEnabled, Converter={StaticResource BoolToOpacity}}"
                                                Text="{Binding Notes, UpdateSourceTrigger=PropertyChanged}"
                                                ToolTip="Note specifiche (es. Fanuc R30iB, Siemens S7-1500)" />
                                        </StackPanel>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </Border>
                </Expander>

            </StackPanel>
        </ScrollViewer>
    </DockPanel>
</Window>
