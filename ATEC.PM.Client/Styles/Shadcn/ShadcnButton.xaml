<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!--
        ===================================================================
        BUTTON STYLES & TEMPLATES (SHADCN UI)
        ===================================================================
    -->

    <!--  Base Button Style  -->
    <Style x:Key="ShadcnButtonBase" TargetType="Button">
        <Setter Property="Height" Value="36" />
        <Setter Property="Padding" Value="16,0" />
        <Setter Property="FontSize" Value="13" />
        <Setter Property="FontWeight" Value="Medium" />
        <Setter Property="FontFamily" Value="{DynamicResource ShadcnFontFamily}" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
    </Style>

    <!--  1. Default (Primary / Dark) Button  -->
    <Style
        x:Key="ShadcnButtonDefault"
        BasedOn="{StaticResource ShadcnButtonBase}"
        TargetType="Button">
        <Setter Property="Background" Value="{DynamicResource ShadcnPrimaryBrush}" />
        <Setter Property="Foreground" Value="{DynamicResource ShadcnPrimaryForegroundBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource ShadcnPrimaryBrush}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border
                        x:Name="border"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="6"
                        SnapsToDevicePixels="True">
                        <ContentPresenter
                            Margin="{TemplateBinding Padding}"
                            HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="#27272A" /> 
                            <!--  Tailwind Zinc 800  -->
                            <Setter TargetName="border" Property="BorderBrush" Value="#27272A" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="border" Property="Background" Value="#09090B" /> 
                            <!--  Tailwind Zinc 950  -->
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="border" Property="Background" Value="#E4E4E7" />
                            <Setter TargetName="border" Property="BorderBrush" Value="#E4E4E7" />
                            <Setter Property="Foreground" Value="#A1A1AA" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--  2. Secondary Button  -->
    <Style
        x:Key="ShadcnButtonSecondary"
        BasedOn="{StaticResource ShadcnButtonBase}"
        TargetType="Button">
        <Setter Property="Background" Value="{DynamicResource ShadcnSecondaryBrush}" />
        <Setter Property="Foreground" Value="{DynamicResource ShadcnSecondaryForegroundBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource ShadcnSecondaryBrush}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border
                        x:Name="border"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="6"
                        SnapsToDevicePixels="True">
                        <ContentPresenter
                            Margin="{TemplateBinding Padding}"
                            HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="#E4E4E7" /> 
                            <!--  Tailwind Zinc 200  -->
                            <Setter TargetName="border" Property="BorderBrush" Value="#E4E4E7" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="border" Property="Background" Value="#D4D4D8" /> 
                            <!--  Tailwind Zinc 300  -->
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="border" Property="Background" Value="#F4F4F5" />
                            <Setter TargetName="border" Property="BorderBrush" Value="#F4F4F5" />
                            <Setter Property="Foreground" Value="#A1A1AA" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--  3. Outline Button  -->
    <Style
        x:Key="ShadcnButtonOutline"
        BasedOn="{StaticResource ShadcnButtonBase}"
        TargetType="Button">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{DynamicResource ShadcnForegroundBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource ShadcnBorderBrush}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border
                        x:Name="border"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="6"
                        SnapsToDevicePixels="True">
                        <ContentPresenter
                            Margin="{TemplateBinding Padding}"
                            HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{DynamicResource ShadcnSecondaryBrush}" />
                            <Setter TargetName="border" Property="BorderBrush" Value="#D4D4D8" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="border" Property="Background" Value="#E4E4E7" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="border" Property="BorderBrush" Value="#E4E4E7" />
                            <Setter Property="Foreground" Value="#A1A1AA" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--  4. Ghost Button  -->
    <Style
        x:Key="ShadcnButtonGhost"
        BasedOn="{StaticResource ShadcnButtonBase}"
        TargetType="Button">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{DynamicResource ShadcnForegroundBrush}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border
                        x:Name="border"
                        Background="{TemplateBinding Background}"
                        CornerRadius="6"
                        SnapsToDevicePixels="True">
                        <ContentPresenter
                            Margin="{TemplateBinding Padding}"
                            HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{DynamicResource ShadcnSecondaryBrush}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="border" Property="Background" Value="#E4E4E7" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="#A1A1AA" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--  5. Destructive Button  -->
    <Style
        x:Key="ShadcnButtonDestructive"
        BasedOn="{StaticResource ShadcnButtonBase}"
        TargetType="Button">
        <Setter Property="Background" Value="{DynamicResource ShadcnDestructiveBrush}" />
        <Setter Property="Foreground" Value="{DynamicResource ShadcnDestructiveForegroundBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource ShadcnDestructiveBrush}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border
                        x:Name="border"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="6"
                        SnapsToDevicePixels="True">
                        <ContentPresenter
                            Margin="{TemplateBinding Padding}"
                            HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="#DC2626" /> 
                            <!--  Tailwind Red 600  -->
                            <Setter TargetName="border" Property="BorderBrush" Value="#DC2626" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="border" Property="Background" Value="#B91C1C" /> 
                            <!--  Tailwind Red 700  -->
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="border" Property="Background" Value="#FCA5A5" />
                            <Setter TargetName="border" Property="BorderBrush" Value="#FCA5A5" />
                            <Setter Property="Foreground" Value="#FEE2E2" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--  6. Link Button  -->
    <Style
        x:Key="ShadcnButtonLink"
        BasedOn="{StaticResource ShadcnButtonBase}"
        TargetType="Button">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{DynamicResource ShadcnPrimaryBrush}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <TextBlock
                        x:Name="text"
                        Margin="{TemplateBinding Padding}"
                        HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                        VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                        Text="{TemplateBinding Content}" />
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="text" Property="TextDecorations" Value="Underline" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="text" Property="Foreground" Value="#A1A1AA" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>


    <!--
        ===================================================================
        BADGES (PILLS INDICATORS)
        We use Styles targetted at Border containing TextBlock
        ===================================================================
    -->

    <!--  Base Badge Style  -->
    <Style x:Key="ShadcnBadgeBase" TargetType="Border">
        <Setter Property="CornerRadius" Value="10" /> 
        <!--  Rounded-full visual effect  -->
        <Setter Property="Padding" Value="10,2" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="HorizontalAlignment" Value="Left" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
    </Style>

    <!--  Default Badge (Dark)  -->
    <Style
        x:Key="ShadcnBadge"
        BasedOn="{StaticResource ShadcnBadgeBase}"
        TargetType="Border">
        <Setter Property="Background" Value="{DynamicResource ShadcnPrimaryBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource ShadcnPrimaryBrush}" />
        <Style.Resources>
            <Style TargetType="TextBlock">
                <Setter Property="Foreground" Value="{DynamicResource ShadcnPrimaryForegroundBrush}" />
                <Setter Property="FontSize" Value="11" />
                <Setter Property="FontWeight" Value="SemiBold" />
                <Setter Property="FontFamily" Value="{DynamicResource ShadcnFontFamily}" />
            </Style>
        </Style.Resources>
    </Style>

    <!--  Secondary Badge (Light Gray)  -->
    <Style
        x:Key="ShadcnBadgeSecondary"
        BasedOn="{StaticResource ShadcnBadgeBase}"
        TargetType="Border">
        <Setter Property="Background" Value="{DynamicResource ShadcnSecondaryBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource ShadcnSecondaryBrush}" />
        <Style.Resources>
            <Style TargetType="TextBlock">
                <Setter Property="Foreground" Value="{DynamicResource ShadcnSecondaryForegroundBrush}" />
                <Setter Property="FontSize" Value="11" />
                <Setter Property="FontWeight" Value="SemiBold" />
                <Setter Property="FontFamily" Value="{DynamicResource ShadcnFontFamily}" />
            </Style>
        </Style.Resources>
    </Style>

    <!--  Outline Badge (Border only)  -->
    <Style
        x:Key="ShadcnBadgeOutline"
        BasedOn="{StaticResource ShadcnBadgeBase}"
        TargetType="Border">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderBrush" Value="{DynamicResource ShadcnBorderBrush}" />
        <Style.Resources>
            <Style TargetType="TextBlock">
                <Setter Property="Foreground" Value="{DynamicResource ShadcnForegroundBrush}" />
                <Setter Property="FontSize" Value="11" />
                <Setter Property="FontWeight" Value="SemiBold" />
                <Setter Property="FontFamily" Value="{DynamicResource ShadcnFontFamily}" />
            </Style>
        </Style.Resources>
    </Style>

    <!--  Destructive Badge (Red)  -->
    <Style
        x:Key="ShadcnBadgeDestructive"
        BasedOn="{StaticResource ShadcnBadgeBase}"
        TargetType="Border">
        <Setter Property="Background" Value="{DynamicResource ShadcnDestructiveBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource ShadcnDestructiveBrush}" />
        <Style.Resources>
            <Style TargetType="TextBlock">
                <Setter Property="Foreground" Value="{DynamicResource ShadcnDestructiveForegroundBrush}" />
                <Setter Property="FontSize" Value="11" />
                <Setter Property="FontWeight" Value="SemiBold" />
                <Setter Property="FontFamily" Value="{DynamicResource ShadcnFontFamily}" />
            </Style>
        </Style.Resources>
    </Style>

    <!--  Dark Badge (Pill style for dark tables)  -->
    <Style
        x:Key="ShadcnBadgeDark"
        BasedOn="{StaticResource ShadcnBadgeBase}"
        TargetType="Border">
        <Setter Property="Background" Value="#18181B" />
        <Setter Property="BorderBrush" Value="#27272A" />
        <Setter Property="CornerRadius" Value="10" />
        <Setter Property="Padding" Value="8,2" />
        <Style.Resources>
            <Style TargetType="TextBlock">
                <Setter Property="Foreground" Value="#A1A1AA" />
                <Setter Property="FontSize" Value="11" />
                <Setter Property="FontWeight" Value="Medium" />
                <Setter Property="FontFamily" Value="{DynamicResource ShadcnFontFamily}" />
            </Style>
        </Style.Resources>
    </Style>

    <!-- ================================================================
         ICON ACTION BUTTON — bottone-icona per la colonna azioni delle
         tabelle (griglia a slot fissi). Il colore si imposta con Background,
         il glifo Segoe MDL2 con Content. Sostituisce il ControlTemplate
         ripetuto su ogni azione.
         Uso: <Button Style="{StaticResource ShadcnIconActionButton}"
                      Background="#059669" Content="&#xE8FB;" ToolTip="..." Tag="{Binding Id}" Click="..."/>
         ================================================================-->
    <Style x:Key="ShadcnIconActionButton" TargetType="Button">
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Width" Value="30" />
        <Setter Property="Height" Value="26" />
        <Setter Property="Margin" Value="1,0" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Foreground" Value="White" />
        <Setter Property="FontFamily" Value="Segoe MDL2 Assets" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Padding="4,2" Background="{TemplateBinding Background}">
                        <TextBlock
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            FontFamily="{TemplateBinding FontFamily}"
                            FontSize="{TemplateBinding FontSize}"
                            Foreground="{TemplateBinding Foreground}"
                            Text="{TemplateBinding Content}" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
