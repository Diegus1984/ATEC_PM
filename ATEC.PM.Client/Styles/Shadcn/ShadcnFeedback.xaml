<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!--
        ===================================================================
        FEEDBACK & OVERLAY COMPONENTS (SHADCN UI)
        ===================================================================
    -->

    <!--  1. ToolTip Style  -->
    <Style x:Key="ShadcnToolTip" TargetType="ToolTip">
        <Setter Property="Background" Value="{DynamicResource ShadcnPrimaryBrush}" />
        <Setter Property="Foreground" Value="{DynamicResource ShadcnPrimaryForegroundBrush}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="8,4" />
        <Setter Property="FontSize" Value="11" />
        <Setter Property="FontFamily" Value="{DynamicResource ShadcnFontFamily}" />
        <Setter Property="HasDropShadow" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToolTip">
                    <Border
                        Padding="{TemplateBinding Padding}"
                        Background="{TemplateBinding Background}"
                        CornerRadius="4"
                        SnapsToDevicePixels="True">
                        <ContentPresenter Content="{TemplateBinding Content}" ContentTemplate="{TemplateBinding ContentTemplate}" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>


    <!--  2. Alert Styles (Border targets containing TextBlocks)  -->

    <!--  Base Alert Style  -->
    <Style x:Key="ShadcnAlertBase" TargetType="Border">
        <Setter Property="CornerRadius" Value="6" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="16,14" />
        <Setter Property="Margin" Value="0,8" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
    </Style>

    <!--  Info Alert (Blue)  -->
    <Style
        x:Key="ShadcnAlertInfo"
        BasedOn="{StaticResource ShadcnAlertBase}"
        TargetType="Border">
        <Setter Property="Background" Value="#EFF6FF" /> 
        <!--  Blue 50  -->
        <Setter Property="BorderBrush" Value="#93C5FD" /> 
        <!--  Blue 300  -->
        <Style.Resources>
            <!--  Title  -->
            <Style x:Key="AlertTitle" TargetType="TextBlock">
                <Setter Property="FontWeight" Value="SemiBold" />
                <Setter Property="FontSize" Value="13" />
                <Setter Property="Foreground" Value="#1E3A8A" /> 
                <!--  Blue 900  -->
                <Setter Property="Margin" Value="0,0,0,2" />
            </Style>
            <!--  Message  -->
            <Style x:Key="AlertDescription" TargetType="TextBlock">
                <Setter Property="FontSize" Value="12.5" />
                <Setter Property="Foreground" Value="#1E40AF" /> 
                <!--  Blue 800  -->
            </Style>
        </Style.Resources>
    </Style>

    <!--  Success Alert (Green)  -->
    <Style
        x:Key="ShadcnAlertSuccess"
        BasedOn="{StaticResource ShadcnAlertBase}"
        TargetType="Border">
        <Setter Property="Background" Value="#ECFDF5" /> 
        <!--  Green 50  -->
        <Setter Property="BorderBrush" Value="#6EE7B7" /> 
        <!--  Green 300  -->
        <Style.Resources>
            <Style x:Key="AlertTitle" TargetType="TextBlock">
                <Setter Property="FontWeight" Value="SemiBold" />
                <Setter Property="FontSize" Value="13" />
                <Setter Property="Foreground" Value="#065F46" /> 
                <!--  Green 900  -->
                <Setter Property="Margin" Value="0,0,0,2" />
            </Style>
            <Style x:Key="AlertDescription" TargetType="TextBlock">
                <Setter Property="FontSize" Value="12.5" />
                <Setter Property="Foreground" Value="#047857" /> 
                <!--  Green 800  -->
            </Style>
        </Style.Resources>
    </Style>

    <!--  Warning Alert (Amber/Yellow)  -->
    <Style
        x:Key="ShadcnAlertWarning"
        BasedOn="{StaticResource ShadcnAlertBase}"
        TargetType="Border">
        <Setter Property="Background" Value="#FFFBEB" /> 
        <!--  Amber 50  -->
        <Setter Property="BorderBrush" Value="#FDE68A" /> 
        <!--  Amber 300  -->
        <Style.Resources>
            <Style x:Key="AlertTitle" TargetType="TextBlock">
                <Setter Property="FontWeight" Value="SemiBold" />
                <Setter Property="FontSize" Value="13" />
                <Setter Property="Foreground" Value="#78350F" /> 
                <!--  Amber 900  -->
                <Setter Property="Margin" Value="0,0,0,2" />
            </Style>
            <Style x:Key="AlertDescription" TargetType="TextBlock">
                <Setter Property="FontSize" Value="12.5" />
                <Setter Property="Foreground" Value="#92400E" /> 
                <!--  Amber 800  -->
            </Style>
        </Style.Resources>
    </Style>

    <!--  Destructive Alert (Red)  -->
    <Style
        x:Key="ShadcnAlertDestructive"
        BasedOn="{StaticResource ShadcnAlertBase}"
        TargetType="Border">
        <Setter Property="Background" Value="#FEF2F2" /> 
        <!--  Red 50  -->
        <Setter Property="BorderBrush" Value="#FCA5A5" /> 
        <!--  Red 300  -->
        <Style.Resources>
            <Style x:Key="AlertTitle" TargetType="TextBlock">
                <Setter Property="FontWeight" Value="SemiBold" />
                <Setter Property="FontSize" Value="13" />
                <Setter Property="Foreground" Value="#7F1D1D" /> 
                <!--  Red 900  -->
                <Setter Property="Margin" Value="0,0,0,2" />
            </Style>
            <Style x:Key="AlertDescription" TargetType="TextBlock">
                <Setter Property="FontSize" Value="12.5" />
                <Setter Property="Foreground" Value="#991B1B" /> 
                <!--  Red 800  -->
            </Style>
        </Style.Resources>
    </Style>

    <!--  3. Spinner Loading Style (Indeterminate Spinner Wheel)  -->
    <Style x:Key="ShadcnSpinner" TargetType="Control">
        <Setter Property="Width" Value="16" />
        <Setter Property="Height" Value="16" />
        <Setter Property="Foreground" Value="{DynamicResource ShadcnPrimaryBrush}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Control">
                    <Viewbox Width="{TemplateBinding Width}" Height="{TemplateBinding Height}">
                        <Canvas
                            x:Name="CanvasRoot"
                            Width="24"
                            Height="24"
                            RenderTransformOrigin="0.5,0.5">
                            <Canvas.RenderTransform>
                                <RotateTransform x:Name="SpinnerRotate" Angle="0" />
                            </Canvas.RenderTransform>
                            <!--  Circle arc with a gap  -->
                            <Path
                                Data="M 12 2 A 10 10 0 0 1 22 12 A 10 10 0 0 1 12 22 A 10 10 0 0 1 3 10"
                                Stroke="{TemplateBinding Foreground}"
                                StrokeEndLineCap="Round"
                                StrokeStartLineCap="Round"
                                StrokeThickness="3" />

                            <!--  Continuous rotation animation  -->
                            <Canvas.Triggers>
                                <EventTrigger RoutedEvent="Canvas.Loaded">
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation
                                                RepeatBehavior="Forever"
                                                Storyboard.TargetName="SpinnerRotate"
                                                Storyboard.TargetProperty="Angle"
                                                From="0"
                                                To="360"
                                                Duration="0:0:0.8" />
                                        </Storyboard>
                                    </BeginStoryboard>
                                </EventTrigger>
                            </Canvas.Triggers>
                        </Canvas>
                    </Viewbox>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
