# ── Build .NET ──────────────────────────────────────────────
[Bb]in/
[Oo]bj/
[Ll]og/
[Ll]ogs/
publish/
artifacts/
TestResults/

# ── Visual Studio / Rider / VS Code ─────────────────────────
.vs/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json.example
*.user
*.suo
*.userosscache
*.sln.docstates
*.rsuser
.idea/
*.DotSettings.user

# ── Config locale / segreti ─────────────────────────────────
appsettings.json
appsettings.*.json
!appsettings.Development.json.example
appsettings.Secrets.json
*.pfx
*.snk

# ── Tooling AI locale (non parte del prodotto) ──────────────
.claude/
.cursor/

# ── Documenti operativi sensibili (usare secret store) ──────
ROADMAP.md
roadmap.md
roadmap_danea.md
**/ROADMAP.md
**/roadmap*.md

# ── Backup / temporanei editor ──────────────────────────────
*.bak
*.tmp
*.swp
*~
*.Backup*
*.csproj.Backup*
*_wpftmp.csproj

# ── Log e dump ──────────────────────────────────────────────
*.log
*.dmp

# ── Node / frontend web ─────────────────────────────────────
node_modules/
atec-pm-web/dist/
atec-pm-web/.env
atec-pm-web/.env.local

# ── OS ──────────────────────────────────────────────────────
Thumbs.db
ehthumbs.db
Desktop.ini
.DS_Store

# ── Graphify (knowledge graph server+web) ───────────────────
# Ogni graphify-out ANNIDATO (cache di scansione lasciate nei sorgenti) è ignorato...
**/graphify-out/
# ...ma alla ROOT del repo si tracciano solo il grafo interrogabile, il report e i
# label delle community (condivisi col team). graph.html (~5MB), l'analysis e le cache
# restano ignorati e rigenerabili con `graphify cluster-only .`.
!/graphify-out/
/graphify-out/*
!/graphify-out/graph.json
!/graphify-out/GRAPH_REPORT.md
!/graphify-out/.graphify_labels.json
!/graphify-out/.graphify_labels.json.sig
