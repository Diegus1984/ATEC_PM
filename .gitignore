# ── Fuori dal repo (decisione 27/07/2026, vedi commit a8cf2a2) ──────
# Sorgenti del client WPF ritirato: 86 MB, in git resterebbero per sempre.
backups/
# SPA compilata servita dal Server in Release: 71 MB rigenerabili con
# `npm run build` in atec-pm-web. Se il deploy in LAN dovesse copiare il repo
# sul server SENZA ricompilare, togliere questa riga e committare wwwroot.
ATEC.PM.Server/wwwroot/
# Copie manuali «prima di toccare» lasciate accanto ai sorgenti.
*.original

# ── Build .NET ──────────────────────────────────────────────
[Bb]in/
[Oo]bj/
[Ll]og/
[Ll]ogs/
publish/
artifacts/
TestResults/

# ── Visual Studio / Rider / VS Code ─────────────────────────
.vs/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json.example
*.user
*.suo
*.userosscache
*.sln.docstates
*.rsuser
.idea/
*.DotSettings.user

# ── Config locale / segreti ─────────────────────────────────
appsettings.json
appsettings.*.json
!appsettings.Development.json.example
appsettings.Secrets.json
*.pfx
*.snk

# ── Tooling AI locale (non parte del prodotto) ──────────────
.claude/
.cursor/

# ── Documenti operativi sensibili (usare secret store) ──────
ROADMAP.md
roadmap.md
roadmap_danea.md
**/ROADMAP.md
**/roadmap*.md

# ── Backup / temporanei editor ──────────────────────────────
*.bak
*.tmp
*.swp
*~
*.Backup*
*.csproj.Backup*
*_wpftmp.csproj

# ── Log e dump ──────────────────────────────────────────────
*.log
*.dmp

# ── Node / frontend web ─────────────────────────────────────
node_modules/
atec-pm-web/dist/
atec-pm-web/.env
atec-pm-web/.env.local

# ── OS ──────────────────────────────────────────────────────
Thumbs.db
ehthumbs.db
Desktop.ini
.DS_Store

# ── Temporanei di lavoro e allegati segnalazioni (20/08/2026) ──
_bug_atts/
_tmp/
_tmp_*.png
__pycache__/
seq_tmp.txt

# Pacchetto di deploy generato (327 MB, rigenerabile con carica-installazione)
deploy/out/

# One-off import/audit del catalogo robot Gamma: password MySQL locale cablata,
# già eseguiti, restano solo su questo PC. L'unico tool vivo è segnalazioni.py.
tools/*.py
!tools/segnalazioni.py
